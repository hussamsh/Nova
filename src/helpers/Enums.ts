

export enum Events {
    INITIALIZATING = "initializing",
    READ_IMAGE = "read_image",
    RESIZING_IMAGE = "resize_image",
    PROGRESS = "progress",
    FAILED = "failed",
    FINISHED = "finished",
    CANCELED = "canceled",   
}
